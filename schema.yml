version: 2

models:
  - name: stg_cc_parcel
    columns:
      - name: Parcel_id
        tests:
          - not_null
  - name: stg_cc_parcel_products
    columns:
      - name: parcel_id
        tests:
          - not_null
